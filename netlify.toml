[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-audit --no-fund"

# Official adapter for Next.js App Router (SSR/ISR/routing on Netlify).
[[plugins]]
  package = "@netlify/plugin-nextjs"
